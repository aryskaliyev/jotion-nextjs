"use client";

import { useMutation } from "convex/react";

interface TitleProps {
    initialData: Doc<"documents">;
};

export const Title = ({
    initialData: Doc<"documents">;
}: TitleProps) => {
    return (
        <div>
            Title
        </div>
    );
};